# 退出保存补充验收

生成时间：2026-09-12T02:02:04+08:00。

本次修正录制暂停／恢复切换时退出可能跳过保存的问题。退出检查先拒绝忙碌操作和自动测试，普通录制退出则等待保存，并再次确认录制和保存均已结束。

## 当前版本证据

- 源码与实际安装包指纹一致：`5ff7a3b26c9af8e86b3f8f6d32d9730cea0952450a12c0a6ca1f4529936b7167`。
- `swift test`：39 项通过、0 失败。新增两个回归用例直接调用应用退出委托，验证“录制切换中”和“设备测试录制中”均拒绝退出。
- release 构建、安装前旧签名 DR 兼容性、安装后 `codesign --verify --deep --strict` 均通过。继续使用原 Apple Development 身份和固定安装路径。
- 在实际安装的应用中点击开始录制，看到计时后暂停，再通过 Command-Q 退出。操作系统进程检查确认应用退出；新增 MP4 为 18.200 秒、1920 × 1080、30 fps，含视频及音频轨，FFmpeg 完整解码成功。
- 随后重新启动并查看实际界面，应用在录制页空闲待用，配置保持 1080p、30 fps、不限时长、3 秒倒计时。

## 与首次报告的关系

[首次完整设备测试](2026-09-12-first-delivery.md)对应前一源码指纹，其 17 项通过和 3 项摄像头失败属于该轮历史证据。本次仅修改退出处理，并运行全部单元／媒体测试及真实录制退出验收；没有将旧设备自检冒充为当前版本重跑结果。

摄像头近黑原始输入仍待确认光线／遮挡情况；后台物理快捷键和菜单栏操作仍待人工验证。系统额外录屏确认状态仍未知，工具明确禁止检查系统通知中心，未绕过。完整目标仍未全部验收通过。

原始录制、文件名、个人路径、签名信息和运行日志仅留本机，不上传。
